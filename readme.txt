Python version : 3.10

do pip install -r requirements.txt

Trained on ASL Alphabet dataset (https://www.kaggle.com/datasets/grassknoted/asl-alphabet) for first 1500 images of each class

Trained for 50 epochs (batch size 32) only